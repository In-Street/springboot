int selectMaxId();
    List<SbTopic> selectByIDRange(SbTopic topic);



     <select id="selectMaxId" resultType="java.lang.Integer">
        select max(id)
        from sb_topic
      </select>

      <select id="selectByIDRange" resultMap="BaseResultMap" parameterType="com.daishumovie.dao.model.SbTopic" >
        select
        <include refid="Base_Column_List" />

        from sb_topic
        where
        sb_topic.id>=#{id,jdbcType=INTEGER} and sb_topic.id &lt; (#{id,jdbcType=INTEGER}+100)
      </select>