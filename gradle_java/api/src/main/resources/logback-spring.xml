<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <include resource="org/springframework/boot/logging/logback/defaults.xml" />
    <property name="LOG_FILE" value="${LOG_FILE:-${LOG_PATH:-${LOG_TEMP:-${java.io.tmpdir:-/tmp}}}/spring.log}"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml" />

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <File>D:/gradle-java-api.log</File>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <FileNamePattern>D:/gradle-java-api-%d{yyyy-MM-dd}.log</FileNamePattern>
            <MaxHistory>2</MaxHistory>
        </rollingPolicy>
        <layout class="ch.qos.logback.classic.PatternLayout">
            <pattern>${FILE_LOG_PATTERN}</pattern>
        </layout>
    </appender>

    <appender name="FILE_PROD" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <File>/data/logs/springboot/api-prod.log</File>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <FileNamePattern>/data/logs/blog-api-prod-%d{yyyy-MM-dd}.log</FileNamePattern>
            <MaxHistory>2</MaxHistory>
        </rollingPolicy>
        <layout class="ch.qos.logback.classic.PatternLayout">
            <pattern>${FILE_LOG_PATTERN}</pattern>
        </layout>
    </appender>

<!-- 添加自定义 请求参数： requestId  -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!--  %logger{56}.%method:%L-->
            <Pattern>%date{yyyy-MM-dd HH:mm:ss.SSS} %-5level - [%X{requestId}] - %msg%n</Pattern>
        </encoder>
    </appender>

    <!-- 开发环境 -->
    <springProfile name="dev,test">

        <logger name="cyf.gradle.api" level="debug"/>
        <logger name="cyf.gradle.dao" level="debug" />
        <!--<logger name="org.apache.ibatis.cache" level="info" />-->
        <!--<logger name="org.apache.ibatis.executor" level="debug" />-->

        <root level="INFO">
<!--            <appender-ref ref="CONSOLE" />-->
            <appender-ref ref="STDOUT" />
            <appender-ref ref="FILE" />
        </root>
    </springProfile>


    <!-- prod环境 -->
    <springProfile name="prod">

        <logger name="cyf.gradle.api" level="debug"/>
        <logger name="cyf.gradle.dao" level="debug" />
        <!--<logger name="org.apache.ibatis.cache" level="info" />-->
        <!--<logger name="org.apache.ibatis.executor" level="debug" />-->

        <root level="INFO">
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="FILE_PROD" />
        </root>
    </springProfile>


</configuration>

